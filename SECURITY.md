# 0.13 源码预览安全状态

2026-09-12 对 0.12 本地研究候选包进行了独立审核。0.13 已在本地实现以下 SEC-01/02 修复，源码独立复核、45 项自动测试与构建检查已通过，尚未完成完整真机回归。本仓库只提供源码、固定厂商依赖和文档/构建测试，不附原 0.12 APK 或新 APK，不创建 GitHub Release。

| 编号 | 历史风险 | 0.13 实现与当前状态 |
| --- | --- | --- |
| SEC-01 | P1：0.12 的 `SdkProbeActivity` 无权限导出，错误实例可能清理现有连接服务并覆盖状态。 | 诊断 Activity 改为不导出，并增加会话归属检查，限制非持有实例清理或写入共享状态；本地修复已实现，源码独立复核、45 项自动测试与构建检查已通过。不能把代码修改等同于已通过所有生命周期真机回归。 |
| SEC-02 | P2：0.12 的 `StreamSmokeActivity` 无权限导出；曾留下有效诊断样本且已配置 Key 时，外部 App 可触发 ASR。该路径不会打开手机麦克风。 | Activity 改为不导出，阻断普通外部 App 启动这条诊断路径；增加用户上传确认和单次启动保护，以同一锁协调销毁与流创建/启动；本地修复已实现，源码独立复核、45 项自动测试与构建检查已通过。App 内用户主动诊断仍可能调用云，未宣称移除全部云诊断能力。 |
| SEC-03 | P2：调试构建不等于生产加固。 | `debuggable=true` 仍保留，授权调试访问可读取应用私有状态。Keystore 不消除运行进程的调试风险；这不等于普通 App 能直接解密全部凭据。 |

源码依据：`app/AndroidManifest.xml` 的诊断组件导出状态、`app/src/SdkProbeActivity.java` 的会话归属及清理路径，以及相关会话控制实现。独立审核历史及验证进度见 [AUDIT](AUDIT.md)。0.12 的测试 APK 仅是本地历史证据，其哈希和真机结果不能代表 0.13。

0.13 的 `lab.py run --execute` 会在安装或操作手机前拒绝非导出的诊断启动路径。请自行构建、安装并从手机 App 启动；不要为了兼容旧脚本重新导出组件。当前 debug 构建仍支持经用户授权的 ADB `run-as` 状态读取和现有会话命令，这不代表上述组件继续向普通 App 导出。

通知监听服务有系统 `BIND_NOTIFICATION_LISTENER_SERVICE` 权限保护。观察台只绑定本机回环，但可能显示问答、通知正文和标识，见 [隐私说明](docs/PRIVACY.md)。诊断样本、录音和服务配置不随源码分发。

历史候选包的指定凭据模式扫描未命中，不等于完整秘密识别或厂商二进制安全认证；新源码目录仍需单独检查。厂商再分发授权和逐文件权属核对另见 [许可范围](docs/LICENSING.md)，不能以非商业声明替代授权。

实际构建产物的 Manifest 和纯逻辑会话隔离检查已通过；外部启动拒绝的真机确认，以及 App 内部连接、语音、录音和通知回归仍待完成。当前不标为生产加固版本；后续验证完成后应在审核记录中更新实际结果，不直接沿用旧版本身份。

Android 官方依据：[导出组件风险](https://developer.android.com/privacy-and-security/risks/android-exported)、[可调试应用风险](https://developer.android.com/privacy-and-security/risks/android-debuggable)。

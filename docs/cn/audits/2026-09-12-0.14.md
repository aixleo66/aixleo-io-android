[简体中文](../../cn/audits/2026-09-12-0.14.md) | [English](../../en/audits/2026-09-12-0.14.md)

# 0.14 交互修复与验证 · 2026-09-12

本记录对应 `0.14-interaction-fixes` 的[修复提交 e867a1e](https://github.com/aixleo66/rayneo-io-android/commit/e867a1e2c52b8a2520fedc5d29f24ad036b44984)，代码及测试清单固定在[该提交的 source-manifest.json](https://github.com/aixleo66/rayneo-io-android/blob/e867a1e2c52b8a2520fedc5d29f24ad036b44984/source-manifest.json)。结论是 **代码已修复，自动检查与构建通过；真机专项回归、外部独立复核待完成**。这不是完整设备验收或生产加固声明。

## 审核来源与处置

使用者提供了另一台电脑对 `76b23962c6705125bfc44912133f99f83de298e7` 的独立审核，提出 F1–F5 五项 P2 和 D1 一项 P3。本机复核确认相关调用链、状态转移、数据流及 API 拒绝条件。外部报告中的独立构建和真机未覆盖范围属于旧提交证据，不自动覆盖本次修复。

| 编号 | 修正 | 当前验证 |
| --- | --- | --- |
| F1 | 文件/样本和手机诊断全链路共用取消对象；读取输入可关闭，ASR 和下一次问答前后检查取消，取消手机任务同时停止对应采集 | 受控读取阻塞、读取后取消、ASR 返回时取消、正常成功与失败路径通过；尚未真实上传或验证手机录音停止时延 |
| F2 | stop 越过 busy/pending 门槛，并取消手机任务、使旧等待回调失效 | 执行实际前端方法体，覆盖语音等待、录音准备、普通请求中断及旧回调重入；蓝牙断开时延待真机验证 |
| F3 | 本地停止与设备 type4 共用幂等收尾入口，首次进入安装绑定录音 ID 的 30 秒完成期限；重复事件不延期，完成后不回退到 stopping | 实际录音控制类在虚拟调度下通过缺失 type6、重复停止、ACK 失败、旧 ID 超时、完成后停止等测试；超时释放 busy、保留原件且不伪造 WAV 成功 |
| F4 | 用同一个回答对象维护手机全文和眼镜文本，所有成功入口统一更新；新任务与取消清理投递内容 | 实际渲染和发送方法体通过 A→B→手动发送 B，以及清理后禁止发送、全文/短答分离测试；镜片内容待确认 |
| F5 | 中英文构建与兼容性说明明确 Android 12+ / API 31 才能连接；保留 API 29 安装声明 | 源码连接检查和编译后 Manifest 核对；没有新增 Android 10/11 连接支持，也未新增 API 31–35 真机覆盖 |
| D1 | 增加 `verify_source.py`，按指定提交的 Git blob 字节校验其清单 | CRLF 工作区、未提交清单修改、提交内容不匹配、历史提交和缺失引用的检查通过；修复提交的 87 项哈希全部匹配 |

## 自动测试与构建

- 既有 45 项加新增 14 项，共 **59 项通过**，本次完整运行约 15.093 秒。未操作设备或调用真实 ASR、模型、Gateway；部分既有测试使用本机 HTTP mock。
- 交互测试使用虚拟 Handler、简单 UI/JSON/存储替身，并执行选取的实际 Activity 方法体和实际 `GlassesRecorder` 状态逻辑。它不是 Android 生命周期模拟器，不验证系统权限弹窗、真实线程调度、蓝牙、MediaCodec 或播放质量。
- 测试中的解码替身仅为编译/状态边界服务，不能作为 WAV 已通过真实解码的证据；正常保存、播放和五分钟边界仍需真机专项回归。
- 使用本机既有 JDK 21 / Android SDK 36 和新建的本地测试签名构建成功；签名、zipalign 和固定厂商 payload 内部条目检查通过。没有重新验收全新机器在线下载工具链。
- 编译后 Manifest 为 versionCode 14、`0.14-interaction-fixes`；四个诊断 Activity 仍不导出，通知服务仍有系统权限保护，主界面正常导出，debug 能力保留。
- 本轮本地 APK SHA256：`cab7d659c45a0012de51339eafb55cb39b88112f1dc5b6bbf1d8594210e902cb`。它仅标识本轮测试签名产物，不附在仓库中，不要求与其他签名的 APK 同哈希。

## 尚待完成

1. 手机文件读取/转写期间取消，以及手机诊断 ASR 返回前取消，确认没有新增后续提交。
2. 语音等待、录音准备和请求执行中断开，确认连接终止、旧回调不恢复错误状态。
3. 眼镜主动结束录音、正常结束保存、异常完成回报、再次开始录音及播放。
4. 先得到回答 A，再得到手机诊断回答 B，手动发送并核对镜片为 B。
5. 修复后正常连接、连续语音、熄屏、通知的回归，以及外部独立审核。

当前是源码预览，不创建 GitHub Release，不提供测试签名私钥、APK、真实凭据或用户数据。厂商再分发授权与逐文件权属事项没有因这些修复而闭合。返回[审核索引](../AUDIT.md)；通用操作见[测试指南](../TESTING.md)。

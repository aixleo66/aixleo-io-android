[简体中文](../../cn/audits/2026-09-12-0.13.md) | [English](../../en/audits/2026-09-12-0.13.md)

> 历史快照：以下完成项、待办、哈希和测试数量均截至 2026-09-12，不能作为后续提交的自动验收。记录在审核当时“未完成”的事项不会随新版本自动改成完成；后续结果另建记录。返回[审核索引](../AUDIT.md)。

# 独立审核与 0.13 修复状态 · 2026-09-12

本记录对象为 **0.13 源码预览**：SEC-01/02 已在本地实现诊断入口隔离与会话归属修复，源码独立复核、45 项自动测试与构建检查已通过；完整真机回归尚未完成，SEC-03 调试能力仍保留。Git 仓库仅含源码、固定厂商依赖、文档与构建测试，不附 APK，不创建 GitHub Release。下面的 0.12-rc1/rc2 审核、哈希及 42 项测试均为历史结果，不代表 0.13 已验证。

## 0.13 已完成与待完成的验证

- 45 项自动测试通过，包含外部入口声明、重复/过期会话所有权、并发抢占和旧诊断 CLI 在接触设备前拒绝的回归；没有云调用。
- 安全 agent 独立复核 SEC-01/02 修复：诊断页面不导出、归属检查、独立清理步骤、主线程超时决策，以及 ASR 显式确认/销毁取消协调，未发现本次修复范围内新的阻断问题。
- 本地 0.13 构建成功，签名和对齐检查通过，厂商 payload 保持固定哈希。实际 APK 的四个诊断 Activity 为 exported=false，主界面及受系统权限保护的通知服务保持预期声明。
- 当前版本为新代码，不沿用 0.12 的 APK 身份；本地构建 APK SHA256 为 `09b1c817cd2885626f63c6fbb405f64f250a3d39507b9cc75190ab626bebbd9e`，该 APK 不附在源码仓库中。
- 审核时无 ADB 设备连接。外部调用被系统拒绝的真机验证、连接/连续语音/熄屏/录音/通知回归尚未完成，不能称为已完成全部验证。上传目录已完成文件清单与指定凭据扫描，未命中；这不是完整秘密识别保证。

独立源码快照重建也已通过：使用审核者新建的临时签名及外部配置，classes.dex、Manifest、vendor payload 与本机最终 0.13 构建逐字节一致，签名和对齐通过。源码快照不包含审核产生的 out 目录、APK、配置或私钥。快照清单见[审核对应提交的 source-manifest.json](https://github.com/aixleo66/rayneo-io-android/blob/9e7e3fea481b6980a6d5e7c16544cc50985a1d8e/source-manifest.json)。

## 历史审核对象

审核对象为本地研究候选包 0.12-rc1。三个独立子 agent 分别检查完整性/构建、文档/使用流程、安全/来源许可；没有安装到手机、触发真实设备或调用云端。随后由主 agent 整理 rc2 的文档和工具包修订，保留 rc1 及其测试身份。

## 0.12 历史发现与处置

| 编号 | 级别 | 问题 | rc2 状态 |
| --- | --- | --- | --- |
| SEC-01 | P1 | 无保护诊断入口的错误清理可干扰现有连接服务和状态 | rc2 未修复；0.13 状态见本文详细安全评估 |
| SEC-02 | P2 | 留有诊断样本且配置 Key 时，外部 App 可触发 ASR | 未修复，必要前提已明确 |
| SEC-03 | P2 | debug APK 尚未生产加固 | 保留研究基线，未修复 |
| DOC-01 | P2 | 首次地址、实时开关、通知仅监听默认值缺少操作指引 | BUILDING.md 补齐 |
| PKG-01 | P2 | stream_smoke.py 依赖私有配置/样本及未声明第三方库，直接执行会调用云 | 从候选包移除，内部历史文件不随包 |
| DOC-02 | P2 | 手机预置语音按钮在新安装缺样本 | 标明不可用及选择自有音频的替代入口；APK 按钮未改 |
| DOC-03 | P2 | 缺观察台操作、测试复现与证据边界 | 新增 OBSERVER.md、TESTING.md |
| DOC-04 | P2 | 自建知识库 Gateway 兼容条件不清 | 新增协议及取消/断线边界说明 |
| DOC-05 | P3 | 历史未发布来源文件和已附许可的状态不清 | PROVENANCE.md、THIRD_PARTY_NOTICES.md 修订 |
| PKG-02 | P3 | bootstrap/help 残留私有 NAS 和相同签名要求 | 仅修订提示文案 |

## 0.12 历史独立构建结果

rc1 ZIP CRC、外部 SHA256、包内逐文件哈希全部通过。在独立解压目录新建临时调试签名，使用已有固定 JDK/SDK 构建成功，签名和对齐验证通过；没有使用原私钥。42 项测试通过。

重建的 `classes.dex`、`assets/vendor-payload.jar`、`AndroidManifest.xml` 与原已测试 APK 逐字节相同。附带 APK SHA256 保持 `14a3125722f60ff2dc514a10c695f77ba0f11b9a1c8e9f2d1f11a1831924eae2`。没有在全新 Windows 上重新下载工具链或重复全部硬件测试。

rc2 当时未修改 App Java/Manifest 或 APK，只修订公开文档、工具提示并排除不完整的历史工具。其 `release-manifest.json` 和 `SHA256SUMS.txt` 留存在本地历史候选包中，不是当前源码预览的构建证明。原 APK 的安全缺陷不能因重新打包而视为修复。

rc2 定向独立复核也已通过：34 个 App 源码/Manifest/APK/payload 文件与 rc1 相同，构建脚本只改两处提示；使用 stage 外部配置及审核新建签名再次构建，关键三项字节相同，签名/对齐通过。该轮未重复 42 项测试。文档与安全说明分别由对应审核 agent 复核通过。

## 权属和公开发布边界

Turbo IO 的参考与许可证已经标示，WebSocket/SLF4J 许可原文已附带；厂商 DEX 固定来源及哈希已记录。厂商再分发授权和逐文件权属核对仍未完成。非商业、研究用途和上游曾附带二进制，都不能单独证明我们获得再分发授权。

历史指定凭据模式扫描未发现泄漏，不等于完整秘密或二进制安全审计。0.13 已实现 SEC-01/02 修复并完成上述源码与构建检查，真机回归仍待完成。当前源码预览不附旧候选包或安装包；Git 上传的实际状态单独记录，不创建 GitHub Release。

## 当时的详细安全评估

2026-09-12 对 0.12 本地研究候选包进行了独立审核。0.13 已在本地实现以下 SEC-01/02 修复，源码独立复核、45 项自动测试与构建检查已通过，尚未完成完整真机回归。本仓库只提供源码、固定厂商依赖和文档/构建测试，不附原 0.12 APK 或新 APK，不创建 GitHub Release。

| 编号 | 历史风险 | 0.13 实现与当前状态 |
| --- | --- | --- |
| SEC-01 | P1：0.12 的 `SdkProbeActivity` 无权限导出，错误实例可能清理现有连接服务并覆盖状态。 | 诊断 Activity 改为不导出，并增加会话归属检查，限制非持有实例清理或写入共享状态；本地修复已实现，源码独立复核、45 项自动测试与构建检查已通过。不能把代码修改等同于已通过所有生命周期真机回归。 |
| SEC-02 | P2：0.12 的 `StreamSmokeActivity` 无权限导出；曾留下有效诊断样本且已配置 Key 时，外部 App 可触发 ASR。该路径不会打开手机麦克风。 | Activity 改为不导出，阻断普通外部 App 启动这条诊断路径；增加用户上传确认和单次启动保护，以同一锁协调销毁与流创建/启动；本地修复已实现，源码独立复核、45 项自动测试与构建检查已通过。App 内用户主动诊断仍可能调用云，未宣称移除全部云诊断能力。 |
| SEC-03 | P2：调试构建不等于生产加固。 | `debuggable=true` 仍保留，授权调试访问可读取应用私有状态。Keystore 不消除运行进程的调试风险；这不等于普通 App 能直接解密全部凭据。 |

源码依据：`app/AndroidManifest.xml` 的诊断组件导出状态、`app/src/SdkProbeActivity.java` 的会话归属及清理路径，以及相关会话控制实现。独立审核历史及验证进度见 [AUDIT](../AUDIT.md)。0.12 的测试 APK 仅是本地历史证据，其哈希和真机结果不能代表 0.13。

0.13 的 `lab.py run --execute` 会在安装或操作手机前拒绝非导出的诊断启动路径。请自行构建、安装并从手机 App 启动；不要为了兼容旧脚本重新导出组件。当前 debug 构建仍支持经用户授权的 ADB `run-as` 状态读取和现有会话命令，这不代表上述组件继续向普通 App 导出。

通知监听服务有系统 `BIND_NOTIFICATION_LISTENER_SERVICE` 权限保护。观察台只绑定本机回环，但可能显示问答、通知正文和标识，见 [隐私说明](../PRIVACY.md)。诊断样本、录音和服务配置不随源码分发。

历史候选包的指定凭据模式扫描未命中，不等于完整秘密识别或厂商二进制安全认证；新源码目录仍需单独检查。厂商再分发授权和逐文件权属核对另见 [许可范围](../LICENSING.md)，不能以非商业声明替代授权。

实际构建产物的 Manifest 和纯逻辑会话隔离检查已通过；外部启动拒绝的真机确认，以及 App 内部连接、语音、录音和通知回归仍待完成。当前不标为生产加固版本；后续验证完成后应在审核记录中更新实际结果，不直接沿用旧版本身份。

Android 官方依据：[导出组件风险](https://developer.android.com/privacy-and-security/risks/android-exported)、[可调试应用风险](https://developer.android.com/privacy-and-security/risks/android-debuggable)。

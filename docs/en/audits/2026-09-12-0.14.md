[简体中文](../../cn/audits/2026-09-12-0.14.md) | [English](../../en/audits/2026-09-12-0.14.md)

# 0.14 interaction fixes and validation · 2026-09-12

This record covers `0.14-interaction-fixes` at [fix commit e867a1e](https://github.com/aixleo66/aixleo-io-android/commit/e867a1e2c52b8a2520fedc5d29f24ad036b44984). Code and test hashes are pinned in [that commit's source-manifest.json](https://github.com/aixleo66/aixleo-io-android/blob/e867a1e2c52b8a2520fedc5d29f24ad036b44984/source-manifest.json). Status: **code fixed; automated checks and build passed; focused device regression and external independent review pending**. This is not full device acceptance or production hardening.

## Audit source and disposition

The user supplied an independent audit from another computer of `76b23962c6705125bfc44912133f99f83de298e7`, reporting five P2 findings F1–F5 and one P3 finding D1. Local review confirmed the relevant call chains, state transitions, data flows, and API rejection condition. Independent builds and uncovered device cases in that external report concern the old commit and do not automatically validate these fixes.

| ID | Change | Current validation |
| --- | --- | --- |
| F1 | File/sample and phone diagnostic stages share one cancellation object. Input can be closed; cancellation is checked around ASR and subsequent Q&A, and cancelling the phone job stops its capture. | Controlled blocked input, cancellation after reading and at ASR return, normal success, and failure paths passed; no real uploads or phone capture-stop latency measured. |
| F2 | stop bypasses busy/pending gates, cancels the phone job, and invalidates old wait callbacks. | Actual frontend method bodies tested during voice waiting, recording preparation, pending requests, and stale callback re-entry; Bluetooth disconnect latency needs device testing. |
| F3 | Local stop and device type4 share an idempotent stop entry point. A 30-second completion deadline is armed once per recording ID; repeats do not extend it and completion cannot regress to stopping. | Actual recorder control logic tested with virtual scheduling for missing type6, repeated stops, ACK failure, stale-ID timeout, and stop after completion. Timeout releases busy, preserves raw data, and does not fake WAV success. |
| F4 | One answer object holds phone and lens text. Every success path updates it; new jobs and cancellation clear delivery content. | Actual rendering/sending method bodies passed A→B→manual send B, cleared-result rejection, and full/short answer separation; lens confirmation is pending. |
| F5 | Chinese and English build/compatibility guides specify Android 12+ / API 31 for connection, retaining the API 29 installation declaration. | Source connection guard and compiled Manifest checked. No Android 10/11 connection support or new API 31–35 device coverage added. |
| D1 | Add `verify_source.py` to check a selected commit's manifest against Git blob bytes. | Checks passed for CRLF working trees, uncommitted manifest edits, committed content mismatch, historical commits, and missing refs. All 87 source hashes at the fix commit match. |

## Automated tests and build

- **59 tests passed**: 45 existing and 14 new checks, approximately 15.093 seconds for the full run. No device or real ASR/model/Gateway calls; some existing tests use local HTTP mocks.
- Interaction tests use a virtual Handler and simple UI/JSON/storage substitutes while executing selected actual Activity method bodies and actual `GlassesRecorder` state logic. This is not an Android lifecycle emulator and does not validate permission dialogs, real scheduling, Bluetooth, MediaCodec, or playback quality.
- The test decoder substitute serves compilation/state boundaries only. It is not evidence of successful real WAV decoding. Normal save/playback and the five-minute limit still need focused device regression.
- Building with the existing local JDK 21 / Android SDK 36 and a newly generated local test signing key succeeded. Signature, zipalign, and pinned vendor payload entry checks passed. Fresh-machine online toolchain download was not revalidated.
- The compiled Manifest declares versionCode 14 and `0.14-interaction-fixes`. Four diagnostic Activities remain non-exported; the notification service retains system permission protection, the main UI remains exported, and debug capabilities remain.
- Local APK SHA256: `cab7d659c45a0012de51339eafb55cb39b88112f1dc5b6bbf1d8594210e902cb`. This identifies only this test-signed artifact, which is not included in the repository. Differently signed APKs need not share its hash.

## Pending work

1. Cancel during phone file reading/transcription and before phone diagnostic ASR returns; confirm no new subsequent submissions.
2. Disconnect during voice waiting, recording preparation, and request execution; confirm termination and no stale callback restoring incorrect state.
3. Test device-initiated recording stop, normal save, abnormal completion reporting, starting another recording, and playback.
4. Obtain answer A, then phone diagnostic answer B, manually send, and confirm B on the lenses.
5. Regress normal connection, consecutive voice turns, screen-off operation, and notifications, and obtain external independent review.

This is a source preview, with no GitHub Release, test signing private key, APK, real credentials, or user data included. These fixes do not resolve vendor redistribution authorization or file-level ownership review. Return to the [audit index](../AUDIT.md); see the [test guide](../TESTING.md) for general procedures.
